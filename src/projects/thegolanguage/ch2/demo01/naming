/*
	命名：
	函数，变量，常量，类型，语句标号，包 的命名，以字母开头，并区分大小写，

	访问级别：
	包级：函数外定义的变量，包内所有文件可访问
	函数级：函数内定义的变量，只能再函数内部访问

	可见性：
	函数外定义的变量，并且以大写字母开头的名字，那么它将是导出的，也就是说可以被外部的包访问

	名字风格
	尽量使用短小的名字，对于局部变量尤其是这样.

	声明：
	var 声明 变量
	const 声明 常量
	type 声明 类型
	func  声明 函数
*/
